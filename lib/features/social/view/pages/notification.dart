import 'package:flutter/material.dart';

class FeedNotificationPage extends StatelessWidget {
  const FeedNotificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
