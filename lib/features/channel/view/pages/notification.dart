import 'package:flutter/material.dart';

class MetubeNotificationPage extends StatelessWidget {
  const MetubeNotificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
