import 'package:flutter/material.dart';

class MetubeLivesPage extends StatelessWidget {
  const MetubeLivesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
