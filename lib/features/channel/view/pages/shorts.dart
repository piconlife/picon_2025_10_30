import 'package:flutter/material.dart';

class MetubeShortsPage extends StatelessWidget {
  const MetubeShortsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
