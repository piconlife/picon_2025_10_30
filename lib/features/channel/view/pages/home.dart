import 'package:flutter/material.dart';

class MetubeHomePage extends StatelessWidget {
  const MetubeHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
