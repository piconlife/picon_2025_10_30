import 'package:flutter/material.dart';

class MarketNotificationPage extends StatelessWidget {
  const MarketNotificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
