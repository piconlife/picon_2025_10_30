import 'package:flutter/material.dart';

class AddUserStoryPage extends StatelessWidget {
  const AddUserStoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
